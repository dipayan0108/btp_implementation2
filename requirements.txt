# Core Python dependencies used by the active scripts in this repository.
# Recommended interpreter: Python 3.7 (64-bit), because the bundled CARLA
# package is ./carla/carla-0.9.8-py3.7-win-amd64.egg and the simulation
# scripts dynamically load an egg matching the current Python major/minor.
# If you want to use Python 3.10+, the CARLA package must be replaced too.

numpy==1.21.6
opencv-python==4.11.0.86
pandas==1.1.5
pygame==2.1.2
tensorboard==2.10.1
tensorflow==2.10.0
tensorflow-probability==0.18.0
torch==1.12.0
